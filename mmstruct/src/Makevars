PKG_libs = -lgomp -lpthread
PKG_CPPFLAGS = -g -fopenmp -DSCYTHE_DEBUG=3 -DSCYTHE_COMPILE_DIRECT -DSCYTHE_RPACK -DSCYTHE_PTHREAD -pthread -DHAVE_TRUNC  -DHAVE_TRUNC
PKG_CXXFLAGS = -Wno-write-strings
OBJECTS.include.scythestat.rng = include/scythestat/rng/lecuyer.o
OBJECTS.root = simulateEKOP.o simulateEKOPMis.o

OBJECTS = $(OBJECTS.include.scythestat.rng) $(OBJECTS.root)

all: before $(SHLIB) after

clean: 
	rm -f $(OBJECTS)

before: clean

after:
